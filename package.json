{
  "name": "edupath",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev --filter client",
    "client": "cd client && npm run dev",
    "build": "netlify build --filter client",
    "build-dev": "netlify build --filter client --context dev",
    "deploy-dev": "netlify deploy --filter client --alias=dev",
    "deploy-prod": "netlify deploy --filter client --prod"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "client"
  ],
  "devDependencies": {
    "@types/node": "^20.8.9",
    "netlify-cli": "^16.9.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}