{
  "name": "edupath",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev --filter client",
    "client": "cd client && npm run dev",
    "build": "netlify build --filter client",
    "bdd": "npm run build-deploy:dev",
    "build-deploy:dev": "npm run build:dev && npm run deploy:dev",
    "build:dev": "netlify build --filter client --context development",
    "deploy:dev": "netlify deploy --filter client",
    "deploy:prod": "netlify deploy --filter client --prod"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "client"
  ],
  "devDependencies": {
    "@types/node": "^20.8.9",
    "netlify-cli": "^16.9.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "vitest": "^1.0.2"
  }
}